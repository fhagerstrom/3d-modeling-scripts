// Create GUI window
proc CreateGUI()
{
    string $window = `window -title "Cogwheel GUI" -widthHeight 300 150 simpleWindow`;
    columnLayout -adjustableColumn true;
    button -label "Click Me" -command "print(\"Button Clicked!\");";
    showWindow $window;
}

CreateGUI()